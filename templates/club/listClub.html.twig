<a href="{{ path('app_AddClub') }}">Add Club</a>
<h3>Liste Club</h3>
 {% for i in tabClubs %}
 id: {{ i.id }}
 name: {{ i.name }}
 description: {{ i.description }}
<a href="{{ path('app_updateClub',{'id':i.id}) }}">Update Club</a>
<a href="{{ path('app_removeClub',{'id':i.id}) }}">Delete Club</a>

<br>
{% endfor %}